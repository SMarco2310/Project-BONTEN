# TODO:

He said we should use function so that the code will be modular.
also the classes to allow reusability of some component and reduce repetition.

I added the functions so we can reuse them.(check the controllers)

I think it will be ideal to change the name controller to api or have a diferent folder called api as this php thing does not like have endpoints but just path to some files like /api/users or /api/products.
